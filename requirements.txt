fastapi[all]>=0.115
# opentelemetry-instrumentation-fastapi <0.64b0 crashes on FastAPI >=0.137
# (_IncludedRouter has no .path). Keep contrib >=0.64b0 until that floor moves.
pydantic>=2.0
pydantic-settings

# Geo / Raster  (P4: services/geo, services/eo-data)
rasterio>=1.3        # packages/geo (P4), ml/io (P3) -- GeoTIFF read + reproject
shapely
pyproj
boto3

# ML  (P3: ml/)
numpy>=1.24          # ml/ (P3) -- raster maths
scipy>=1.10          # ml/pipeline (P3) -- connected components, morphology
pystac-client
prometheus-client

uvicorn
sqlalchemy
geoalchemy2
asyncpg
psycopg2-binary
redis
celery
python-jose[cryptography]
# OTel core + instrumentation must be same release train; contrib 0.64b0+
# is required for FastAPI >=0.137 (_IncludedRouter fix).
opentelemetry-api>=1.43
opentelemetry-sdk>=1.43
opentelemetry-instrumentation-fastapi>=0.64b0
opentelemetry-instrumentation-httpx>=0.64b0
opentelemetry-exporter-otlp>=1.43
python-json-logger
httpx
tenacity
pytest
pytest-asyncio
aiosqlite

# Dev / CI tooling
black
flake8
mypy
types-requests
bandit
import-linter

# Agent loop (P2)
langgraph
langchain-core
