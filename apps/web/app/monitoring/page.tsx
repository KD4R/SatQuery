import { MonitoringScreen } from "../../components/mission/MonitoringScreen";

export const metadata = { title: "Monitoring — SatQuery AI" };

export default function MonitoringPage() {
  return <MonitoringScreen />;
}
