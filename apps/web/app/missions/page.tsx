import { MissionMemory } from "../../components/mission/MissionMemory";

export const metadata = { title: "Missions — SatQuery AI" };

export default function MissionsPage() {
  return <MissionMemory />;
}
